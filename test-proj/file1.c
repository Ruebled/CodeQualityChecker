#include <stdio.h>

int global_var;  // Global variable declaration
int global_var2 = 5;  // Global variable declaration

int main() {
    int a, b, c = 10;  // Multiple variable declarations on the same line
    float x = 2.5;
    int y = 100; 
    goto label;  // Goto usage
}

